import { RawSourceMap } from 'source-map';
import { LineMapping } from '../types';
export declare function getSourceMap(jsCode: string): RawSourceMap | undefined;
/**
 * Returns a custom line mapper which maps `node_modules` sources to `null`.
 */
export declare function generateLineMapping(map: RawSourceMap | undefined): LineMapping;
