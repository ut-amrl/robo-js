import { Visitor } from 'babel-traverse';
import * as t from 'babel-types';
export declare enum AppType {
    None = 0,
    Tail = 1,
    Mixed = 2
}
export declare type Labeled<T> = T & {
    labels?: number[];
    appType?: AppType;
    localVars?: t.Identifier[];
};
export declare function getLabels(...nodes: Labeled<t.Node>[]): number[];
export declare function plugin(): {
    visitor: Visitor;
};
