import * as babel from 'babel-core';
import * as types from '../types';
/**
 * Compiles (i.e., "stopifies") a program. This function should not be used
 * directly by clients of Stopify.
 *
 * @param src the program to Stopify
 * @param opts compiler options
 * @returns the stopified program
 */
export declare function compileFromAst(src: babel.types.Program, opts: types.CompilerOpts): string;
/**
 * Compiles (i.e., "stopifies") a program. This function should not be used
 * directly by clients of Stopify.
 *
 * @param src the program to Stopify
 * @param opts compiler options
 * @returns the stopified program
 */
export declare function compile(src: string, opts: types.CompilerOpts): string;
